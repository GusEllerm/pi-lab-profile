{
  "_comment": "MCP servers extensions/mcp.ts starts for every session. hpc-bridge needs uv on PATH and a Globus login on first use; the Globus Labs cluster is in its public registry as a multi-user endpoint, so it attaches with no SSH.",
  "servers": {
    "hpc": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/globus-labs/hpc-bridge", "hpc-bridge"],
      "env": {
        "HPC_BRIDGE_USER_DIR": "~/.pi/agent/hpc-bridge/user",
        "HPC_BRIDGE_STATE_DIR": "~/.pi/agent/hpc-bridge/state",
        "HPC_BRIDGE_OMIT_INSTRUCTIONS": "1"
      },
      "timeoutMs": 180000,
      "skills": [{ "uri": "hpcbridge://guidance/operations", "name": "driving-hpc" }]
    }
  }
}
